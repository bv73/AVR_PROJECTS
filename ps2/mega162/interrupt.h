#define SEK_CONST       (977)     // Значение счетчика для секундного таймера

unsigned int Register_OneSec; // Регистр секундного счетчика
unsigned char SecundaOK;   // Секундный флаг. Если не ноль, то прошла секунда

